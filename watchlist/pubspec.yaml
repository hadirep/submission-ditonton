name: watchlist
description: A new Flutter package project.
publish_to: 'none'
version: 0.0.1
homepage:

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  movie:
    path: ../movie
  tv_series:
    path: ../tv_series
  search:
    path: ../search
  about:
    path: ../about
  dartz: ^0.10.1
  flutter_bloc: ^8.0.1
  firebase_crashlytics: ^2.6.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  mockito: ^5.1.0
  build_runner: ^2.1.10
  bloc_test: ^9.0.3

flutter:
  uses-material-design: true